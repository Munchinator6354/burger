<li>
	{{burger_name}}

	<button class="devour-burger" data-id="{{id}}" data-devour="{{devoured}}">
		{{#if devoured}}Consume!{{else}}Remake the burger{{/if}}
	</button>

	{{!-- <button class="delete-burger" data-id="{{this.id}}">
		DELETE!
	</button> --}}
</li>